#!/usr/bin/env ruby
require 'gauss'
require 'dotenv'

begin
  Dotenv.load(Gauss::GAUSS_KEY)
rescue Dotenv::FormatError
  puts "Your Gauss key configuration file (#{Gauss::GAUSS_KEY}) is invalid."
  exit
end

Gauss.run(ARGV)
