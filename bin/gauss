#!/usr/bin/env ruby
require "gauss"
require "dotenv"

begin
  Dotenv.load(ENV['HOME'] + '/.gauss')
rescue Dotenv::FormatError
  puts "Your Gauss configuration file (" + ENV['HOME'] + "/.gauss) is invalid."
  exit
end

args = ARGV.map(&:to_sym)
args[0] ||= :help
Gauss.run(args)
